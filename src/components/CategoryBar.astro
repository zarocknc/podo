---
import logoB from "@assets/banner/logoB.png";
import podoIsoB from '../assets/banner/pododinamicisoB.png';
import cruzB from '../assets/banner/cruzB.png';
---



<section class="fondoBanner lg:my-3 gap-5  flex  justify-center my-2 text-[#fff]">
    <div class="flex justify-start h-full pt-2"><img src={cruzB.src} alt="" class="lg:w-5 w-3"></div>
    <div class="lg:py-4 py-2 flex justify-center items-center lg:gap-6 gap-3">
        <img src={podoIsoB.src} alt="" class="w-10">
        <span class="lg:text-4xl lg:block hidden">|</span>
        <h2 id="banner" class="lg:text-4xl text-sm uppercase">Te acompañamos en cada paso</h2>
    </div>
    <div class="flex h-full lg:pt-10 pt-8"><img src={cruzB.src} alt="" class="lg:w-5 w-3"></div>
</section>

<script is:inline>
  const phrases = [
    "Te acompañamos en cada paso",
    "plantillas dinámicas personalizadas",
    "análisis de la marchas"
  ];

  let currentIndex = 0;
  const banner = document.getElementById("banner");

  function updateBanner() {
    banner.textContent = phrases[currentIndex];
  }

  function nextPhrase() {
    currentIndex = (currentIndex + 1) % phrases.length;
    updateBanner();
  }

  function startBannerRotation() {
    setInterval(nextPhrase, 4000); // Cambia cada 5 segundos
  }

  // Inicia la rotación del banner al cargar la página
  document.addEventListener("DOMContentLoaded", startBannerRotation);
</script>
<style>
  .fondoBanner{
        background-image: url('../assets/banner/bannerpromoc.png');
        object-fit: cover;
    }
</style>